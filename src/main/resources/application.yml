server:
  port: 8080
spring:
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update
#    properties:
#      hibernate:
#        jdbc:
#          time_zone: UTC
  datasource:
    url: jdbc:postgresql://localhost:5432/library
    username: postgres
    password: user123
    driver-class-name: org.postgresql.Driver